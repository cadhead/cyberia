import { h } from 'preact';
import './loader.scss';

const Loader = () => {
  return (
    <div class="Loader">If the content takes too long to load, try reloading the page.</div>
  );
}

export default Loader;
